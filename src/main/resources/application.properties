# Application Name
spring.application.name=blood-donor-app

# Server Configuration
server.port=${PORT:8080}

# Database Configuration (Railway MySQL)
spring.datasource.url=${DB_URL:jdbc:mysql://crossover.proxy.rlwy.net:23201/railway?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:OxlJRWEDajdsCKqCandTCyFZizRdkhHi}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Database Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=BloodDonorHikariCP
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}

# Email Configuration (Resend API)
resend.api.key=${RESEND_API_KEY:re_eaY3jL5A_FcjVqJTw5YR5xWSH1GJpYgti}
resend.from.email=${RESEND_FROM_EMAIL:onboarding@resend.dev}

# JWT Configuration (Use Environment Variables in Production)
jwt.secret=${JWT_SECRET:your-256-bit-secret-key-here-must-be-at-least-32-characters-long}
jwt.expiration=${JWT_EXPIRATION:86400000}

# OTP Configuration
otp.expiration.minutes=10
otp.length=6

# Report Configuration
report.auto.confirm.hours=24

# CORS Configuration (Use Environment Variables in Production)
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Admin Configuration (Use Environment Variables in Production)
admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:M@noj98491}

# Logging Configuration
logging.level.com.blooddonor=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:DEBUG}
logging.level.org.hibernate.SQL=${SQL_LOG_LEVEL:DEBUG}
logging.level.org.springframework.web=${WEB_LOG_LEVEL:DEBUG}
logging.level.org.springframework.mail=${MAIL_LOG_LEVEL:DEBUG}
logging.level.com.zaxxer.hikari=${HIKARI_LOG_LEVEL:DEBUG}
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Health Checks (Actuator)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized


